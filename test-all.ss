(define (run-all)
  (begin
		(load "A13-test-code.ss")
		(display "A13-tests: ")
		(newline)
		(r)
		(newline)

	(load "A14-test-code.ss")
	(display "A14-tests: ")
	(newline)
	(r)
	(newline)

	(load "A16-test-code.ss")
	(display "A16-tests: ")
	(newline)
	(r)
	(newline)

	(load "A17-test-code.ss")
	(display "A17-tests: ")
	(newline)
	(r)
	(newline)
))

(define r run-all)
